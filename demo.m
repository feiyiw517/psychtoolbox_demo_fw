sca;
close all;
clearvars;

% ???demo????????? PsychDefaultSetup(2)??????????????;
% PsychDefaultSetup(2)

% ???????????????????????????????PTB?????????????
Screen('Preference', 'SkipSyncTests', 1);

% ?????????????????????????????????????????????????0
screens = Screen('Screens');

% ????
screenNumber = max(screens);

% ?????????????????????0?255
white = WhiteIndex(screenNumber);
black = BlackIndex(screenNumber);

% ????????
grey = white / 2;
%% 
% ????????????????rect?????????????????????????X?????Y??????????????????
% ???????windowRect = [0,0,800,400]
rect = [0,0,800,400];
[window, windowRect] = Screen('OpenWindow', screenNumber, grey, rect);

% ?????????????????

% ???????rect?windowRect????
rect = Screen('Rect', window);

% ?????????????????WindowRect?Rect?????
[screenXpixels, screenYpixels] = Screen('WindowSize', window);

% ?????????
% xCenter = screenXpixels / 2;
% yCenter = screenYpixels / 2;
[xCenter, yCenter] = RectCenter(windowRect);

% ???????????inter-frame-interval??????????????????
ifi = Screen('GetFlipInterval', window);

% ???????????????????? ifi = 1 / hertz 
hertz = FrameRate(window);

% pixel size?????????????????????????????????????
pixelSize = Screen('PixelSize', window);

% ?????????????????????????????????????????????0
[width, height] = Screen('DisplaySize', screenNumber);

% ???????????255?
% ???demo????PsychDefaultSetup(2)???????????Screen('ColorRange', window, 1,
% [], 1)???????????1?????demo??????1
maxLum = Screen('ColorRange', window);

% ???????????????
KbStrokeWait;

% "sca" ??? "Screen CloseAll"
sca;
